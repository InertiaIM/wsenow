                <h2 style="display:none;">{{ experience.name }}</h2>
                <h4 style="display:none;">
                    {{ experience.location }}
{% if (experience.startDate is not null) and (experience.endDate is null) %}
                    <br/>{{ experience.startDate | date('F j, Y') }}
{% endif %}
{% if (experience.startDate is not null) and (experience.endDate is not null) %}
                    <br/>{{ experience.startDate | date('F j, Y') }} - {{ experience.endDate | date('F j, Y') }}
{% endif %}
                </h4>
                <div style="margin-top:14px; color:#ffffff; font-size:18px; display:none;">{{ experience.detail | markdown }}</div>